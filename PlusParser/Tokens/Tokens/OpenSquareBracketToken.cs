namespace PlusParser.Tokens.Tokens;

public class OpenSquareBracketToken: TokenBase
{
    public OpenSquareBracketToken(string val, int start, int end, int lineNumber) : base(val, start, end, lineNumber)
    {
    }
}