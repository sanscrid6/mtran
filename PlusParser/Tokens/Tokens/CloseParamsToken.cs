namespace PlusParser.Tokens.Tokens;

public class CloseParamsToken: TokenBase
{
    public CloseParamsToken(string val, int start, int end, int lineNumber) : base(val, start, end, lineNumber)
    {
    }
}