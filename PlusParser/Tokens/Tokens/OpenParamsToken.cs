namespace PlusParser.Tokens.Tokens;

public class OpenParamsToken: TokenBase
{
    public OpenParamsToken(string val, int start, int end, int lineNumber) : base(val, start, end, lineNumber)
    {
    }
}